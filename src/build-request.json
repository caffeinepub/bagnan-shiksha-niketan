{
  "kind": "build_request",
  "title": "Fix gender theme color switching and clean up/animate hero image",
  "priority": "normal",
  "requirements": [
    {
      "id": "REQ-19",
      "text": "Fix the Baby Girl/Baby Boy theme so selecting a gender actually changes the site colors across pages (background, text, cards, buttons, gradients) and persists after refresh/navigation.",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-5"
        ],
        "quotes": [
          "website not changing color  upon selection baby girl or boy"
        ]
      },
      "acceptanceCriteria": [
        "Clicking “Baby Girl” updates the overall site color palette immediately (e.g., background, primary/secondary/accent-based UI elements).",
        "Clicking “Baby Boy” updates the overall site color palette immediately.",
        "The selected theme persists on page refresh and when navigating between routes.",
        "The theme implementation remains compatible with Tailwind’s `hsl(var(--...))` color bindings (i.e., CSS variable values are in a format Tailwind can resolve correctly)."
      ]
    },
    {
      "id": "REQ-20",
      "text": "Integrate the uploaded image `sioj.png` as the Home page hero illustration (replacing the current hero illustration image) and apply a lightweight animation to it.",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-5"
        ],
        "quotes": [
          "animate this image"
        ]
      },
      "acceptanceCriteria": [
        "`sioj.png` is added as a static frontend asset (served from `frontend/public/...`) and is used as the Home page hero image in place of the current hero illustration.",
        "The hero image has a visible, kid-friendly, lightweight animation (e.g., existing `animate-float` or `animate-bounce-gentle`) that does not harm readability/performance.",
        "The image renders once (no duplicate rendering of the same hero image)."
      ]
    },
    {
      "id": "REQ-21",
      "text": "Remove the extra image that appears layered on top of the main image in the UI (the overlay/duplicate image above the hero image).",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-5"
        ],
        "quotes": [
          "and on top of the omage there is another image delte it"
        ]
      },
      "acceptanceCriteria": [
        "On the affected page(s), there is no longer any unintended/extra image positioned on top of the main hero image.",
        "The intended main image remains visible and properly aligned after removal.",
        "No layout overlap or z-index stacking causes a second image to cover the main image."
      ]
    }
  ],
  "constraints": [
    "Do not modify files under `frontend/src/components/ui` (shadcn components are read-only).",
    "Do not modify any immutable paths listed in SYSTEM_CONTEXT (compose around them instead)."
  ],
  "nonGoals": [
    "No backend (Motoko) changes.",
    "No new learning modules or content expansion beyond the requested theme + image fixes."
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  },
  "userProfileUpdate": {
    "goalsToAdd": [],
    "goalsToRemove": [],
    "preferencesToAdd": [],
    "preferencesToRemove": [],
    "miscToAdd": [],
    "miscToRemove": [],
    "fieldsToSet": {},
    "fieldsToDelete": []
  }
}